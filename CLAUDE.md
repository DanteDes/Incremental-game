# Project Rules

- Never commit plan or spec documents generated by Claude (e.g. `docs/superpowers/plans/`, `docs/superpowers/specs/`). Keep them local only — `docs/superpowers/` is gitignored for this reason. If asked to write a design/plan doc, write it there and do not `git add`/commit it unless explicitly told to.
